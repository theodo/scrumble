.member-select(
  ng-repeat="member in members"
  ng-class="{'selected': teamCheck[member.id]}"
)
  label(
    ng-click="check()"
  )
    trello-avatar.member-select-image(
      member="member"
    )
    input(
      type="checkbox"
      ng-model="teamCheck[member.id]"
    )
    
