div.sprint-body(layout="row" layout-align="center end")
  md-card.layout-padding(flex="80")
    h2.md-title(layout-align="center end") SET YOUR PROJECT
    md-input-container(layout="column")
      label Select a board: 
      md-select(ng-model="project.boardId")
        md-option(ng-value="board.id" ng-repeat="board in boards") {{ board.name }}
    
    div(ng-if="project.boardId" layout="column")
      md-input-container
        md-tooltip(md-direction="left") To be used in daily mail
        label Select "blocked" column
        md-select(ng-model="project.columnMapping.blocked")
          md-option(ng-value="column.id" ng-repeat="column in boardColumns") {{ column.name }}
      md-input-container
        md-tooltip(md-direction="left") To be used in daily mail
        label Select "to validate" column
        md-select(ng-model="project.columnMapping.toValidate")
          md-option(ng-value="column.id" ng-repeat="column in boardColumns") {{ column.name }}
      md-input-container
        md-tooltip(md-direction="left") To be used in daily mail
        label Select "doing" column
        md-select(ng-model="project.columnMapping.doing")
          md-option(ng-value="column.id" ng-repeat="column in boardColumns") {{ column.name }}
      md-input-container
        md-tooltip(md-direction="left") To be used in daily mail
        label Select "sprint" column
        md-select(ng-model="project.columnMapping.sprint")
          md-option(ng-value="column.id" ng-repeat="column in boardColumns") {{ column.name }}

      h4.md-title The team
      .select-teams
        .select-dev-team
          div Select the team
          select-people(
            members="boardMembers"
            selected-members="project.team.dev"
          )
      md-content.md-padding(
        layout="row" layout-wrap layout-align="center start" layout-xs="column"
      )
        div(flex="50" flex-sm="100" flex-xs="100" layout="column" ng-repeat="dev in project.team.dev")
          member-form(member="dev" delete="delete(dev)")
    section(layout="row" layout-sm="column" layout-align="center center" layout-wrap)
      md-button.md-warn(ng-click="clearTeam()") CLEAR THE TEAM
      md-button.md-raised.md-primary(ng-click="save()" ng-disabled="saving") Save
